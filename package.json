{
  "name": "taurus-v2",
  "version": "1.0.0",
  "description": "Taurus Trailer Rails API",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && cp -a client/build/. public/",
    "heroku-postbuild": "npm run build && echo 'Client built!'"
  },
  "author": "Cottonwood Coding",
  "license": "MIT"
}
